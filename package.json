{
  "name": "cartographer",
  "private": true,
  "version": "1.0.0",
  "description": "An Age-of-Exploration voyage game: chart a fog-covered sea, then sell your charts or keep them secret.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "package": "npm run build && rm -f cartographer-itch.zip && cd dist && zip -qr ../cartographer-itch.zip ."
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
